How-to Install:
---------------

# Clone the repo
git clone https://github.com/egulhan/dotvim.git ~/.vim

# Create symbolic link for .vimrc
ln -s ~/.vim/vimrc ~/.vimrc

# Install submodules
cd ~/.vim
git submodule init
git submodule update
